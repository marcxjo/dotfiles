# mod key used for most binds
# Mod1 = Alt
# Mod2 = Num Lock
# Mod3 = Shift
# Mod4 = Super
set $mod Mod4

# The files we include below will be created / overwritten by nwg-shell tools
#
include modules/variables.conf
include modules/autostart.conf
include modules/inputs.conf
include modules/font.conf

# nwg-shell sway config
# Read `man 5 sway` for reference.

# border & title
for_window [title="^.*"] border pixel 1, title_format "%app_id >> %title"
default_border pixel 1

# direction keys
set $up k
set $down j
set $left h
set $right l

###############     Autostart      ##################

exec systemctl --user import-environment DISPLAY WAYLAND_DISPLAY SWAYSOCK
exec hash dbus-update-activation-environment 2>/dev/null && \
    dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY SWAYSOCK
exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec ~/.azotebg

### Turn off screen on lid closed
set $laptop eDP-1
bindswitch lid:on output $laptop disable
bindswitch lid:off output $laptop enable

#############         Bindings           ##################

include modules/keybinds.conf

###########          Workspace Bindings          ###############

include modules/outputs.conf

############      Container/Window control  ############

include modules/windows.conf

###############      Border & Gaps     ###############

include modules/chrome.conf

############    application settings   ############

include modules/apps.conf
