#!/bin/sh

hwindows=( $(bspc query -N -d $MON:focused -n .hidden) )

if [[ ${#hwindows[@]} -eq 0 ]]
then
  printf "%s %s %s" "%{A:toggle_hide $MON:}" "" "%{A}"
else
  printf "%s%s %s %s%s" "%{F${color2}}" "%{A:toggle_unhide $MON:}" "" "%{A}" "%{F-}"
fi
