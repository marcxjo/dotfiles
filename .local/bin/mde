#!/bin/bash

if [[ ${#@} -eq 0 ]]; then
  echo "No command specified!"
  exit 1
fi

declare -r MDE_CMD="$1"
shift

declare -r MDE_LIB_DIR="${HOME}/.local/lib/mde"

# shellcheck disable=1090
if ! . "${MDE_LIB_DIR}/${MDE_CMD}.sh" "${@}" 2>/dev/null; then
  echo "No such command!"
  exit 1
fi

exit 0
