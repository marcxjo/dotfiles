#!/bin/sh

# Reload dunst if it has already been started.
if [ "pgrep -c -x dunst" ]
then
	# dunst won't properly reload if we
	# use `pkill` in place of `killall`.
	killall -e dunst > /dev/null 2>&1
fi

exec dunst -config ${HOME}/.local/share/themes/bspwm/dunstrc
