#!/bin/sh

. ~/.cache/wal/bspwm-colors.sh

SB="${color2}"
SF="${color10}"
NB="${colorbg}"
NF="${colorfg}"
FN="Terminus:style=bold:pixelsize=14"
OPTIONS="-c -p -1 -u $NB -U $NF -t $SB -T $SF"

exec tabbed ${OPTIONS} "$@"
